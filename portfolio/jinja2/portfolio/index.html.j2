{% extends 'portfolio/base.html.j2' %}

{% block title %}
    Latest
{% endblock %}

{% block body %}

<div class="container-fluid" style="margin-top: 10px">
    <div class="grid">
        <div class="grid-sizer"></div>
        {% for image in latest_images %}
            <div class="grid-item">
                <a href="{{ static('portfolio/img/latest/' + image) }}" data-toggle="lightbox" data-gallery="example-gallery">
                    <img src="{{ static('portfolio/img/latest/' + image) }}" />
                </a>
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}
